import XCTest
@testable import ios_system_package

final class ios_system_packageTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ios_system_package().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
